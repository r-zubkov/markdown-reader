# Implementation status

| Task | Status | Commit/worktree evidence | Checks | Deviations |
|---|---|---|---|---|
| P00-T01 | completed | Worktree contains `package.json`, `pnpm-lock.yaml`, Vite/TS/ESLint/Vitest/Playwright configs, bootstrap React entry, smoke tests and updated README status. | `corepack pnpm install --frozen-lockfile`; `corepack pnpm dev` + HTTP 200; `corepack pnpm typecheck`; `corepack pnpm lint`; `corepack pnpm test`; `corepack pnpm test:security`; `corepack pnpm test:bench`; `corepack pnpm build`; `corepack pnpm test:e2e:list`; `corepack pnpm exec playwright install --dry-run`. | None. Full Playwright browser execution was intentionally deferred; P00-T01 required config discovery/listing, not feature E2E. DEC-022 later narrowed the release matrix to Chromium. |
| P00-T02 | completed | Worktree contains `src/domain/content` pipeline prototype/limits/types with `auto/h1/h2/h3/whole` layouts, deterministic corpus generator, semantic/security/bench tests, worker typecheck import and `docs/benchmarks/pipeline-spike.md`; `requirements-and-decisions.md` has DFR-001/002 evidence updates. | `corepack pnpm typecheck`; `corepack pnpm lint`; `corepack pnpm test`; `corepack pnpm test:security`; `corepack pnpm test:bench`; `corepack pnpm build`. Extra measurement run: `corepack pnpm exec vitest run --config vitest.bench.config.ts --reporter verbose --silent=false`. | None for task scope. Browser memory/Long Tasks remained for P00-T04/P00-T06/P02-T01, not claimed by this spike. |
| P00-T03 | completed | Worktree contains Dexie storage atomicity spike prototype in `src/infrastructure/db` with validation for all five split layouts, reusable fake-IDB fixtures/tests, Chromium browser IndexedDB confirmation, `.ms-playwright/` gitignore entry and `docs/benchmarks/storage-atomicity-spike.md`; `data-and-state.md` and `requirements-and-decisions.md` carry forward P01-T02 contract findings. | `corepack pnpm exec vitest run src/infrastructure/db/storage-atomicity-spike.test.ts --reporter verbose`; `corepack pnpm typecheck`; `corepack pnpm lint`; `corepack pnpm test`; separately started Vite plus `PLAYWRIGHT_BROWSERS_PATH=.ms-playwright corepack pnpm exec playwright test e2e/storage-atomicity.spec.ts --project=chromium`; `corepack pnpm build`. | No task-scope blocker. `fake-indexeddb@6.2.5` did not preserve Node Blob/typed-array shape in the legacy fixture, so the fake-IDB migration test uses fixture byte fallback while real Chromium confirms production Blob recovery. Playwright's managed webServer teardown hangs only in the Windows/Codex sandbox; browser verification reused a separately started Vite process and stopped that exact PID, without changing normal Playwright/CI behavior. |
| P00-T04 | completed | Worktree contains a 20,000-chunk TanStack window-virtualizer harness, deterministic heterogeneous fixture, bounded 96-item cache, focus pinning, remeasurement controls, component/benchmark/browser tests and `docs/benchmarks/virtual-reader-spike.md`. Supported Chromium scenarios pass with at most 23 mounted chunks, 0.50 px stabilized drift and 33.40 ms measured blank gap. | `corepack pnpm install --frozen-lockfile`; `corepack pnpm typecheck`; `corepack pnpm lint`; `corepack pnpm test`; `corepack pnpm test:security`; `corepack pnpm test:bench`; `corepack pnpm build`; `corepack pnpm test:e2e:list` (7 Chromium tests); separately started Vite plus `PLAYWRIGHT_BROWSERS_PATH=.ms-playwright` and `corepack pnpm test:e2e:all` (7 passed). | DEC-022 limits the MVP matrix to Chrome/Chromium and responsive mobile-Chromium. Firefox, Safari/WebKit and physical iPhone are outside the commitment. P01-T04/P03-T02 are unblocked. |
| P00-T05 | completed | Worktree contains pure semantic mapping/reason/UI-trigger contracts, SHA-256 block fingerprints with `PIPELINE_VERSION=3`, eight deterministic update pairs, same-version layout/property/exhaustiveness tests and `docs/benchmarks/progress-mapping-spike.md`. All update pairs hit the expected block; no false exact case occurred. | Targeted mapping suite (18 passed); `corepack pnpm typecheck`; `corepack pnpm lint`; `corepack pnpm test` (51 passed); `corepack pnpm test:security` (3 passed); `corepack pnpm test:bench` (4 passed); `corepack pnpm build`; storage migration/rebuild regression (9 passed); visible mapping/pipeline benchmark reruns. | The P00-T02 code did not yet contain update pairs despite the shared-corpus precondition; P00-T05 added them as its expected fixture output. The `0.20` structural-similarity proposal remains subject to production-corpus recalibration. P03-T03/P03-T04/P04-T02 mapping work is unblocked. |
| P00-T06 | completed | Worktree contains `aria-nova` shadcn/React Aria configuration, locally reviewed Dialog/Sheet/RadioGroup/FileDropField primitives, responsive mobile platform harness, PWA `generateSW` skeleton, component/axe/Chromium tests and `docs/benchmarks/mobile-ui-platform-spike.md`. | `tsc -b`; `eslint .`; targeted component suite (2 passed); full Vitest suite (53 passed); security suite (3 passed); benchmark suite (4 passed); `vite build` with generated Workbox assets; separately started Vite plus mobile harness (2 passed) and complete Chromium suite (9 passed). | React Aria Components 1.21.1 did not export FileTrigger/DropZone, so a local native file/drop fallback was selected and documented. Physical-device viewport/keyboard/lifecycle checks and production update/offline behavior remain P05 scope. P01-T01/P02-T02/P05-T01/P05-T02 are unblocked. |
| P01-T01 | completed | Worktree contains declarative Library/Reader/not-found route composition, route error boundary and focus policy, semantic shell landmarks/skip links/toolbars, app status region, Russian string catalog, pre-paint theme mirror/provider and responsive shell styles. | Local installed binaries: `tsc -b`; `eslint .`; `vitest run` (8 files, 57 tests); `vite build`; separately started Vite plus `playwright test e2e/app-shell.spec.ts --project=chromium` (1 passed, including 320 px overflow and axe checks). | The theme mirror is intentionally only a pre-paint hint; IndexedDB preference reconciliation remains P01-T02. Full visual theme QA, reader content, TOC and PWA lifecycle remain later tasks. |
| P01-T02 | completed | Worktree contains an application `DocumentRepository` port and a Dexie-backed adapter for stage/batch/commit/abort/cleanup, current metadata/window/state/preferences, runtime record validation, pipeline-version gating, narrow live observation and startup cleanup. Theme reconciliation writes IndexedDB before refreshing the pre-paint localStorage hint. | `corepack pnpm typecheck`; `corepack pnpm lint`; `corepack pnpm test` (9 files, 59 tests); `corepack pnpm test:security`; `corepack pnpm test:bench`; `corepack pnpm build`; separately started Vite plus `PLAYWRIGHT_BROWSERS_PATH=.ms-playwright corepack pnpm exec playwright test e2e/document-repository.spec.ts --project=chromium` (1 passed). | The P00-T03 physical Dexie schema and its v1-to-v2 migration are retained as the production base, rather than duplicating an equivalent schema. Browser Blob persistence is confirmed for the new adapter; fake-IDB remains transaction evidence only. |
| P01-T03 | completed | Worktree contains the versioned import-worker protocol, authoritative Worker UTF-8/hash/pipeline path, serialized coordinator with staging/batch/commit/abort handling, live Library query and accessible file action, plus import protocol/coordinator/browser tests. | `tsc -b`; `eslint .`; `vitest run` (11 files, 64 tests); security suite (3 passed); benchmark suite (4 passed); `vite build`; separately started Vite plus `PLAYWRIGHT_BROWSERS_PATH=.ms-playwright playwright test --project=chromium` (14 passed). | The production pipeline is the safe P00 implementation used as the walking-skeleton pipeline; complete import decisions, full import overlay and final Library presentation remain P02/P04 work. Vite resolves the Worker export condition for Markdown dependencies; dev/test-only P00 diagnostic routes preserve their browser regression harness and are omitted from the production route table. |
