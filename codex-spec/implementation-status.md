# Implementation status

| Task | Status | Commit/worktree evidence | Checks | Deviations |
|---|---|---|---|---|
| P00-T01 | completed | Worktree contains `package.json`, `pnpm-lock.yaml`, Vite/TS/ESLint/Vitest/Playwright configs, bootstrap React entry, smoke tests and updated README status. | `corepack pnpm install --frozen-lockfile`; `corepack pnpm dev` + HTTP 200; `corepack pnpm typecheck`; `corepack pnpm lint`; `corepack pnpm test`; `corepack pnpm test:security`; `corepack pnpm test:bench`; `corepack pnpm build`; `corepack pnpm test:e2e:list`; `corepack pnpm exec playwright install --dry-run`. | None. Full Playwright browser execution was intentionally deferred; P00-T01 required config discovery/listing, not feature E2E. DEC-022 later narrowed the release matrix to Chromium. |
| P00-T02 | completed | Worktree contains `src/domain/content` pipeline prototype/limits/types with `auto/h1/h2/h3/whole` layouts, deterministic corpus generator, semantic/security/bench tests, worker typecheck import and `docs/benchmarks/pipeline-spike.md`; `requirements-and-decisions.md` has DFR-001/002 evidence updates. | `corepack pnpm typecheck`; `corepack pnpm lint`; `corepack pnpm test`; `corepack pnpm test:security`; `corepack pnpm test:bench`; `corepack pnpm build`. Extra measurement run: `corepack pnpm exec vitest run --config vitest.bench.config.ts --reporter verbose --silent=false`. | None for task scope. Browser memory/Long Tasks remained for P00-T04/P00-T06/P02-T01, not claimed by this spike. |
| P00-T03 | completed | Worktree contains Dexie storage atomicity spike prototype in `src/infrastructure/db` with validation for all five split layouts, reusable fake-IDB fixtures/tests, Chromium browser IndexedDB confirmation, `.ms-playwright/` gitignore entry and `docs/benchmarks/storage-atomicity-spike.md`; `data-and-state.md` and `requirements-and-decisions.md` carry forward P01-T02 contract findings. | `corepack pnpm exec vitest run src/infrastructure/db/storage-atomicity-spike.test.ts --reporter verbose`; `corepack pnpm typecheck`; `corepack pnpm lint`; `corepack pnpm test`; separately started Vite plus `PLAYWRIGHT_BROWSERS_PATH=.ms-playwright corepack pnpm exec playwright test e2e/storage-atomicity.spec.ts --project=chromium`; `corepack pnpm build`. | No task-scope blocker. `fake-indexeddb@6.2.5` did not preserve Node Blob/typed-array shape in the legacy fixture, so the fake-IDB migration test uses fixture byte fallback while real Chromium confirms production Blob recovery. Playwright's managed webServer teardown hangs only in the Windows/Codex sandbox; browser verification reused a separately started Vite process and stopped that exact PID, without changing normal Playwright/CI behavior. |
| P00-T04 | completed | Worktree contains a 20,000-chunk TanStack window-virtualizer harness, deterministic heterogeneous fixture, bounded 96-item cache, focus pinning, remeasurement controls, component/benchmark/browser tests and `docs/benchmarks/virtual-reader-spike.md`. Supported Chromium scenarios pass with at most 23 mounted chunks, 0.50 px stabilized drift and 33.40 ms measured blank gap. | `corepack pnpm install --frozen-lockfile`; `corepack pnpm typecheck`; `corepack pnpm lint`; `corepack pnpm test`; `corepack pnpm test:security`; `corepack pnpm test:bench`; `corepack pnpm build`; `corepack pnpm test:e2e:list` (7 Chromium tests); separately started Vite plus `PLAYWRIGHT_BROWSERS_PATH=.ms-playwright` and `corepack pnpm test:e2e:all` (7 passed). | DEC-022 limits the MVP matrix to Chrome/Chromium and responsive mobile-Chromium. Firefox, Safari/WebKit and physical iPhone are outside the commitment. P01-T04/P03-T02 are unblocked. |
| P00-T05 | completed | Worktree contains pure semantic mapping/reason/UI-trigger contracts, SHA-256 block fingerprints with `PIPELINE_VERSION=3`, eight deterministic update pairs, same-version layout/property/exhaustiveness tests and `docs/benchmarks/progress-mapping-spike.md`. All update pairs hit the expected block; no false exact case occurred. | Targeted mapping suite (18 passed); `corepack pnpm typecheck`; `corepack pnpm lint`; `corepack pnpm test` (51 passed); `corepack pnpm test:security` (3 passed); `corepack pnpm test:bench` (4 passed); `corepack pnpm build`; storage migration/rebuild regression (9 passed); visible mapping/pipeline benchmark reruns. | The P00-T02 code did not yet contain update pairs despite the shared-corpus precondition; P00-T05 added them as its expected fixture output. The `0.20` structural-similarity proposal remains subject to production-corpus recalibration. P03-T03/P03-T04/P04-T02 mapping work is unblocked. |
| P00-T06 | completed | Worktree contains `aria-nova` shadcn/React Aria configuration, locally reviewed Dialog/Sheet/RadioGroup/FileDropField primitives, responsive mobile platform harness, PWA `generateSW` skeleton, component/axe/Chromium tests and `docs/benchmarks/mobile-ui-platform-spike.md`. | `tsc -b`; `eslint .`; targeted component suite (2 passed); full Vitest suite (53 passed); security suite (3 passed); benchmark suite (4 passed); `vite build` with generated Workbox assets; separately started Vite plus mobile harness (2 passed) and complete Chromium suite (9 passed). | React Aria Components 1.21.1 did not export FileTrigger/DropZone, so a local native file/drop fallback was selected and documented. Physical-device viewport/keyboard/lifecycle checks and production update/offline behavior remain P05 scope. P01-T01/P02-T02/P05-T01/P05-T02 are unblocked. |
| P01-T01 | completed | Worktree contains declarative Library/Reader/not-found route composition, route error boundary and focus policy, semantic shell landmarks/skip links/toolbars, app status region, Russian string catalog, pre-paint theme mirror/provider and responsive shell styles. | Local installed binaries: `tsc -b`; `eslint .`; `vitest run` (8 files, 57 tests); `vite build`; separately started Vite plus `playwright test e2e/app-shell.spec.ts --project=chromium` (1 passed, including 320 px overflow and axe checks). | The theme mirror is intentionally only a pre-paint hint; IndexedDB preference reconciliation remains P01-T02. Full visual theme QA, reader content, TOC and PWA lifecycle remain later tasks. |
| P01-T02 | completed | Worktree contains an application `DocumentRepository` port and a Dexie-backed adapter for stage/batch/commit/abort/cleanup, current metadata/window/state/preferences, runtime record validation, pipeline-version gating, narrow live observation and startup cleanup. Theme reconciliation writes IndexedDB before refreshing the pre-paint localStorage hint. | `corepack pnpm typecheck`; `corepack pnpm lint`; `corepack pnpm test` (9 files, 59 tests); `corepack pnpm test:security`; `corepack pnpm test:bench`; `corepack pnpm build`; separately started Vite plus `PLAYWRIGHT_BROWSERS_PATH=.ms-playwright corepack pnpm exec playwright test e2e/document-repository.spec.ts --project=chromium` (1 passed). | The P00-T03 physical Dexie schema and its v1-to-v2 migration are retained as the production base, rather than duplicating an equivalent schema. Browser Blob persistence is confirmed for the new adapter; fake-IDB remains transaction evidence only. |
| P01-T03 | completed | Worktree contains the versioned import-worker protocol, authoritative Worker UTF-8/hash/pipeline path, serialized coordinator with staging/batch/commit/abort handling, live Library query and accessible file action, plus import protocol/coordinator/browser tests. | `tsc -b`; `eslint .`; `vitest run` (11 files, 64 tests); security suite (3 passed); benchmark suite (4 passed); `vite build`; separately started Vite plus `PLAYWRIGHT_BROWSERS_PATH=.ms-playwright playwright test --project=chromium` (14 passed). | The production pipeline is the safe P00 implementation used as the walking-skeleton pipeline; complete import decisions, full import overlay and final Library presentation remain P02/P04 work. Vite resolves the Worker export condition for Markdown dependencies; dev/test-only P00 diagnostic routes preserve their browser regression harness and are omitted from the production route table. |
| P01-T04 | completed | Worktree contains the current-version Reader route/use case, a bounded eight-chunk window, a repository-private branded `SanitizedHtml` render boundary, ReaderState semantic-anchor persistence/resolution, Dexie schema v3 migration, controlled recovery states, and Reader unit/browser tests. | `pnpm typecheck`; `pnpm lint`; `pnpm test` (13 files, 68 tests); `pnpm build`; separately started Vite plus `PLAYWRIGHT_BROWSERS_PATH=.ms-playwright pnpm test:e2e` (15 Chromium tests passed). | This is deliberately a minimal bounded window plus explicit save-position action. P03-T02 replaces it with the selected virtualizer behavior; TOC, modes/strategies, observer throttling and full restore confidence handling remain scheduled work. |
| P02-T01 | completed | Worktree contains pipeline version 4 with whole-document reference/footnote resolution before safe HAST chunk partitioning, structural/title limits, deterministic internal links/IDs/fingerprints/layouts, explicit grammar and fallback policy, protocol version 2 byte/count/hash validation, current-pipeline repository validation, source-Blob rebuild orchestration and the production pipeline report. | `corepack pnpm typecheck`; `corepack pnpm lint`; `corepack pnpm test` (14 files, 81 tests); `corepack pnpm test:security` (1 file, 3 tests); `corepack pnpm test:bench` (3 files, 5 tests); `corepack pnpm build`; existing Vite with `reuseExistingServer` plus `PLAYWRIGHT_BROWSERS_PATH=.ms-playwright corepack pnpm test:e2e` (16 Chromium tests passed). | No task-scope deviation. The 1.25 MB file cap and auto-detect policy remain deliberately conservative; increasing either requires new supported-Chromium evidence. Duplicate/update UI, remote-image preference enforcement and cross-version progress mapping remain in their scheduled tasks. |
| P02-T02 | completed | Worktree contains a single responsive O-01 import overlay, an exhaustive stale-transition-rejecting controller reducer, picker/drop parity for one local `.md`, truthful progress/cancel/finalize/error/success states, Russian error/retry copy, and a metadata-only L-01 library with loading, empty, ready and storage-error recovery states. | Workspace-local pnpm binary: `typecheck`; `lint`; `test` (15 files, 84 tests); separately started Vite (PID 23796), then `PLAYWRIGHT_BROWSERS_PATH=.ms-playwright test:e2e` (17 Chromium tests passed, including 320 px/axe import-overlay coverage); `build`. | The overlay uses the selected React Aria Dialog with responsive full-height Sheet styling rather than switching primitives at runtime. O-02 is represented only as a reducer extension hook; exact duplicate/update decision UI remains P02-T03. The sandbox default Corepack cache is inaccessible, so verification used the committed workspace-local pnpm binary. |
| P02-T03 | completed | Worktree contains a pure Unicode identity policy, indexed ready-current hash and normalized title/filename candidate queries, one O-02 overlay state for exact duplicate/multiple update choices, atomic Separate resumption, and an explicit non-mutating Replace handoff pending mapped replacement. | Workspace-local pnpm binary: `typecheck`; `lint`; `test` (16 files, 92 tests); `test:security` (3 passed); `test:bench` (5 passed); `build`; separately started Vite with `PLAYWRIGHT_BROWSERS_PATH=.ms-playwright test:e2e` (all 20 Chromium cases reported passed, including O-02 axe and keyboard selection). | Replace deliberately does not commit or claim success until P04-T02 can map the reader anchor and clean old versions. The Playwright command emitted every passing case but did not return its final summary within the Windows/Codex 30-second tool limit; this follows the documented teardown limitation. P03-T01 is unblocked; P04-T02 remains gated by P03-T04. |
| P03-T01 | completed | Worktree contains repository-validated current-version outlines, pure hash resolver/history adapter, nested H1-H3 TOC, responsive desktop/mobile Sheet UI, hash-priority reader loading, keyboard focus transfer and direct-link E2E coverage. | Workspace-local binaries: `tsc -b`; `eslint .`; targeted Vitest (4 passed); `vite build`; separately started Vite with `PLAYWRIGHT_BROWSERS_PATH=.ms-playwright` plus `playwright test e2e/toc-deep-links.spec.ts --project=chromium` (2 passed, including Reader axe, direct hash, 390 px Sheet and keyboard focus). | The complete Chromium suite was also started with a separately managed Vite process, but the Windows/Codex sandbox did not produce a final teardown summary; no task-target failure was reported. This is the documented sandbox limitation and no Playwright configuration was changed. P03-T02 and P03-T03 are unblocked. |
| P03-T02 | completed | Worktree contains the production `ReaderViewport`, stable version/ordinal virtual keys, a 96-record LRU cache over compound-key Dexie range reads, dynamic measurement and target locking, localized chunk/media fallbacks, focus pinning, responsive TOC scrolling, observed-location output and real-pipeline browser/benchmark coverage. | Workspace-local binaries: `tsc -b`; `eslint .`; Vitest (19 files, 99 tests); security (3 passed); benchmark (4 files, 6 tests); `vite build`; separately managed Vite plus Chromium continuous-reader suite (3 passed) and full Playwright suite (25 passed after the success-overlay test used its intended Open action). Production measurements: 181-chunk repository benchmark 117.15 ms/96 cached; 220-chunk sequential Chromium 16 mounted/82 cached/0 ms blank gap/58 ms longest task/0.09 px theme drift. | No product-scope deviation. The selected P00 configuration remains `useWindowVirtualizer`, overscan 8, `useFlushSync=false`, direct DOM updates off, mounted budget 48 and cache limit 96. P03-T03 is next; P03-T04 can consume `ObservedLocation` after T03. |
| P03-T03 | completed | `HEAD` contains sections rendering through bounded 24-chunk windows, accessible section context/pager, persisted `auto/h1/h2/h3/whole` strategy settings, deterministic cost-based automatic mode and `modeOrigin`, unavailable-whole guidance, responsive settings layout, property/unit coverage and Chromium E2E coverage. | Workspace-local binaries: `tsc -b`; `eslint .`; Vitest (21 files, 103 tests); security (3 passed); benchmark (4 files, 6 tests); `vite build`; separately managed Vite plus `playwright test --project=chromium --grep='P03-T03'` (1 passed, including 320 px, keyboard, reload, axe and overflow). A full 26-scenario Chromium run emitted the P03-T03 success but did not return a final summary before the Windows/Codex teardown limit. | No product-scope deviation. `whole` remains a single logical section backed by bounded chunk windows. Capture/restore dispatch is an integration shell only; P03-T04 owns location-controller persistence and cross-mode mapping. |
| P03-T04 | completed | Worktree contains a semantic top-block observer, 120/800 ms throttled location controller, serialized flushes, same-version exact/approximate/none restore, automatic ReaderState persistence, Library progress and `docs/benchmarks/progress-persistence.md`. | Workspace-local pnpm binary: `typecheck`; `lint`; Vitest (24 files, 109 tests); security (3 passed); benchmark (4 files, 6 tests); `build`; separately managed Vite plus focused P03-T04 Chromium coverage and the complete Chromium suite (27 passed). The fake-clock profile reduces 50 observations to one UI publication and one repository write. | No task-scope deviation. No dependency, schema or pipeline-version change was needed. Cross-version replacement deliberately remains P04-T02 and reuses the accepted mapper/confidence contract. |
| P04-T01 | completed | Worktree contains the exact-ID `deleteDocument` repository port and one Dexie transaction that removes ReaderState, every owned version/chunk and the Document; Library DocumentItem primary/continue/menu actions; a React Aria AlertDialog with safe initial focus, recoverable error state and deterministic cancel/success focus; and focused repository, component and Chromium coverage. | Workspace-local pnpm binary: `typecheck`; `lint`; `test` (25 files, 113 tests); `build`; existing Vite with `PLAYWRIGHT_BROWSERS_PATH=.ms-playwright pnpm test:e2e` (29 Chromium tests passed). Targeted repository/component tests also passed (10 tests); the delete E2E covers axe, 320 px reflow, cancel, successful last-item deletion and a forced IndexedDB failure. | No task-scope deviation. `DOCUMENT_NOT_FOUND` is an idempotent stale-entry result and refreshes the list; no fuzzy deletion, Undo, bulk deletion or replace mapping was added. P04-T02 is next. |
| P04-T02 | completed | Worktree contains captured expected-current/ReaderState replacement, validated pipeline-v3+ fingerprint mapping, one atomic Document/current/ReaderState switch, schema-v4 pending restore notices, completed Replace/Separate flows and idempotent visible/startup cleanup retry. O-02 reports exact/approximate/none and Reader consumes approximate/none notices once. | Workspace-local pnpm binary: `typecheck`; `lint`; `test` (26 files, 132 tests); security (3 passed); benchmark (4 files, 6 tests); `build`; separately managed Vite plus the complete Chromium suite with one worker (33 passed). Production repository tests exercise all eight P00 update pairs, commit rollback/conflict, cleanup retry/idempotence and old-pipeline rejection. | No dependency or Markdown-pipeline change. DB schema 4 adds only the optional version-scoped notice and retains migration coverage. The default eight-worker Chromium run exposed the existing resize-measurement load flake once; the complete single-worker run passed. Cleanup failure leaves only an obsolete non-current ready version and is retried from O-02 or startup. P04-T03 is next. |
| P04-T03 | completed | Worktree contains typed missing/stale/corrupt/empty Reader recovery with safe diagnostic codes, source-Blob reprocess action, safe partial chunk fallback in both continuous and sections windows, no-headings explanation, route fatal recovery, focus-safe recovery headings and the manual accessibility record at `docs/accessibility/P04-T03-reader-recovery.md`. | Workspace-local binaries: `tsc -b`; `eslint .`; Vitest (26 files, 134 tests); security suite (3 passed); `vite build`; separately managed Vite plus the full 33-scenario Chromium suite split into 7 and 26 passing scenarios with one worker. | No schema, pipeline or dependency change. NVDA and VoiceOver execution cannot be performed in the Windows/Codex sandbox; the exact manual checklist and release-audit gap are recorded for P05-T04. The separately managed Vite process could not be stopped because Windows sandbox process-to-port inspection was denied; no project configuration was changed. P05-T01 and P05-T02 are unblocked. |
| P05-T01 | completed | Worktree contains the full semantic light/dark token set, canonical `system/light/dark` preference with pre-paint mirror, OS-theme response, responsive 44 px mobile controls, complete namespaced Reader prose/syntax styling, anchor-preserving theme/viewport remeasurement, two-theme screenshots and `docs/accessibility/P05-T01-theme-responsive.md`. | Workspace-local binaries: `tsc -b`; `eslint .`; Vitest (26 files, 136 tests); security (3 passed); isolated benchmark suite (4 files, 6 tests); `vite build`; separately managed Vite plus the complete one-worker Chromium suite (37 passed). | No dependency or font asset was added; documented system fallbacks remain. The initial parallel benchmark run hit two corpus-test timeouts and passed in the required isolated run. Final accent branding remains `OPEN-001`; physical-device, browser-chrome zoom and assistive-technology release checks remain P05-T04. P05-T02 is next. |
| P05-T02 | completed | Worktree contains a relative-scope PWA manifest and icons, an eight-entry app-shell-only `generateSW` precache, prompt registration, a stable external-store update controller, one responsive Update/Later or offline banner, import/finalize gating, ReaderState flush before one authorized reload, production-only offline E2E and the static-host contract. | Workspace-local pnpm: frozen install; `typecheck`; `lint`; Vitest (27 files, 140 tests); security (3 passed); `build` with eight precache entries; nested `/reader/` base build inspection; separately managed production preview plus `playwright.pwa.config.ts` (1 passed). Existing Chromium regression coverage passed when split: focused continuous-reader suite (3 passed) and the other scenarios across sequential runs; the task-specific production PWA case remained green after the final build. | Added direct MIT dependency `workbox-window@7.4.1` for the page-to-waiting-worker prompt channel. The Windows sandbox's default eight-worker run exhausted timing capacity (30 passed, 7 existing tests timed out); sequential runs exposed pre-existing order-sensitive Reader drift and dark-theme axe flakes, while the same focused suites passed. No normal Playwright/CI behavior was weakened. A real two-bundle update is covered at the controller/adapter boundary rather than by mutating production output during E2E. P05-T03 is next. |
| P05-T03 | completed | Worktree contains capability-safe storage-health normalization, an explicit persistence request, persisted remote-image preference, DOM-before-request media blocking/placeholders and a one-banner priority of storage fatal/risk, update, then offline. `SafeHtmlChunk` remains the only HTML boundary and permits HTTPS/data images only under the active policy. | Workspace-local pnpm: `typecheck`; `lint`; Vitest (29 files, 146 tests); security (3 passed); `build`; separately managed Vite plus `e2e/storage-privacy-status.spec.ts` (1 Chromium case passed); separately managed production preview plus `playwright.pwa.config.ts` (1 case passed). | The `0.90` storage ratio is an advisory early-warning threshold only; no automatic deletion or quota guarantee was added. The default eight-worker Chromium suite reproduced an existing duplicate/update timing failure; the task-specific browser and production-PWA coverage passed. Browser storage APIs remain advisory and the product still has no backup/export. P05-T04 is next. |
| P05-T04 | completed | Worktree contains fail-closed persisted-HTML validation before branding, a deployable CSP/header policy with external theme bootstrap, production CSP/browser regressions, responsive/status-overlap fixes, full-page theme baselines and the evidence record at `docs/accessibility/P05-T04-hardening.md`. | Workspace-local pnpm: `typecheck`; `lint`; Vitest (29 files, 147 tests); security (3 files, 12 tests); benchmark (4 files, 6 tests); `build`; nested `/reader/` build; serialized `test:e2e:all` (41 Chromium + 2 production-PWA passed); dependency audit found no known production vulnerabilities. | Automated hardening is green. The user explicitly waived NVDA + Chrome and real browser-chrome 200%/400% zoom on 2026-09-24; the report preserves the scope and unproven support claims. No timeout or performance budget was raised. P05-T05 is unblocked. |
| P05-T05 | completed | The acceptance checklist is evidence-linked and signed `ACCEPTED`; requirements, task status, roadmap, root README and the release-candidate handoff are reconciled. All 52 normalized requirements map to 25 completed tasks; 61 repository Markdown files have valid local link targets. | Frozen install; `typecheck`; `lint`; Vitest (29 files, 147 tests); security (3 files, 12 tests); benchmark (4 files, 6 tests); `build` with 9 app-shell precache entries; serialized Chromium (41 passed) plus production PWA/CSP (2 passed); production audit found no known vulnerabilities. | No code or dependency change. The user-approved NVDA + Chrome and direct Chrome UI zoom waivers remain explicit and expire before a compatibility claim or material accessibility redesign. VoiceOver/Safari/iOS remains outside DEC-022. `OPEN-001` is the only nonblocking open product item. |
